# 2026-03-12 변경 내역

## 작업 범위
- 포트 단독 매칭으로 발생하던 오탐을 줄임
- 서비스 판별 시 `service.name`, `service.product`를 우선 사용하도록 조정

## 코드 변경 사항
### 1) `analysis/analyzer.py`
- 노출(rule) 매칭 로직 수정
  - 기존: `alias_match or port_match`
  - 변경: `alias_match` 필수
- 변경 효과
  - 포트만 일치해서 finding이 생성되는 동작 제거
  - 포트는 finding 생성 조건이 아니라 confidence 보조 신호로만 사용
- 의도 설명 주석 추가
  - 서비스 메타데이터 근거가 있어야 finding 생성
  - 포트는 confidence 조정용 보조 신호

### 2) `analysis/risk_engine.py`
- 서비스 판별 로직 수정
  - 아래와 같은 포트 단독 강제 판별 제거
    - `3306 -> mysql`
    - `9200 -> elasticsearch`
    - `445 -> samba`
    - `21 -> ftp`
- 판별 기준 변경
  - `service.name`, `service.product` 기반 판별을 우선 적용
  - 메타데이터가 비어 있는 경우에만 `http` 관련 제한적 fallback 유지
- 의도 설명 주석 추가
  - 메타데이터 우선 판별 원칙
  - fallback을 제한적으로 유지하는 이유

## 테스트 변경 사항
### 3) `analysis/tests/test_analyzer.py`
- negative 테스트 4건 추가
  - `9200` 포트라도 elasticsearch/opensearch 이름이 아니면 Elasticsearch finding 미생성
  - `445` 포트라도 samba 관련 이름이 아니면 Samba finding 미생성
  - `3306` 포트라도 mysql/mariadb 이름이 아니면 Database finding 미생성
  - `21` 포트라도 ftp 관련 이름이 아니면 FTP finding 미생성

## 검증 결과
- 실행 명령: `python -m pytest -q analysis/tests`
- 결과: `8 passed`
- 기존 보장 사항 유지
  - Redis/SSH finding 테스트 통과
  - drift 테스트 통과

## 변경 제외 범위
- `analysis/models.py` 미수정
- API 관련 파일 미수정
